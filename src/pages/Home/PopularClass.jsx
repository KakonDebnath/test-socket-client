import SectionTitle from "../../components/SectionTitle/SectionTitle";


const PopularClass = () => {
    return (
        <div>
            <SectionTitle subheading="best course" heading="Popular classes"></SectionTitle>
        </div>
    );
};

export default PopularClass;